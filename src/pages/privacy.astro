---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Icon from '@/components/ui/Icon.astro';

const supportEmail = 'aboyahyadev@icloud.com';
const emailSubject = encodeURIComponent('[MyDinero] Privacy Inquiry');
const mailtoLink = `mailto:${supportEmail}?subject=${emailSubject}`;
---

<BaseLayout
  title="Privacy Policy"
  description="MyDinero Privacy Policy. Your expense tracker, budget planner, and money manager data stays private. No data collection, no tracking, no bank connections required."
>
  <div class="privacy-page">
    <!-- Ambient Background -->
    <div class="page-bg">
      <div class="page-orb page-orb-1"></div>
      <div class="page-orb page-orb-2"></div>
      <div class="page-grid"></div>
    </div>

    <div class="container">
      <!-- Header -->
      <header class="page-header scroll-animate">
        <div class="page-badge">
          <Icon name="lock" size={16} />
          <span>Privacy First</span>
        </div>
        <h1 class="page-title">
          Privacy<br />
          <span class="title-highlight">Policy</span>
        </h1>
        <p class="page-updated">Last updated: February 4, 2026</p>
      </header>

      <article class="privacy-content">
        <!-- Hero Section -->
        <section class="privacy-hero scroll-animate">
          <div class="hero-card">
            <div class="card-glass"></div>
            <div class="card-content">
              <div class="hero-icon">
                <Icon name="lock" size={28} />
              </div>
              <h2>Your Privacy Matters</h2>
              <p class="hero-text">
                MyDinero is built with privacy at its core. Your <strong>expense tracker</strong> data,
                budgets, and financial information belong to you — and only you.
              </p>
              <p>
                We don't collect, store, or sell your personal data. This <strong>budget app</strong> was
                designed so that your <strong>money manager</strong> stays completely private.
              </p>
            </div>
          </div>
        </section>

        <!-- Data Storage -->
        <section class="privacy-section scroll-animate">
          <div class="section-header">
            <span class="section-number">01</span>
            <h2>Data Storage</h2>
          </div>
          <div class="section-content">
            <p>
              All your data in this <strong>spending tracker</strong> is stored locally on your device using
              Apple's secure frameworks. Your transactions, categories, and budgets in the <strong>budget planner</strong>
              never leave your iPhone or iPad unless you choose to enable iCloud sync.
            </p>
            <div class="info-card">
              <div class="info-icon">
                <Icon name="cloud" size={20} />
              </div>
              <div class="info-content">
                <h3>Optional iCloud Sync</h3>
                <p>
                  If you enable iCloud sync, your <strong>expense manager</strong> data is encrypted and
                  synced through your personal Apple Account. We never see, access, or process your synced data —
                  it travels directly between your devices and Apple's servers.
                </p>
              </div>
            </div>
          </div>
        </section>

        <!-- What We Don't Collect -->
        <section class="privacy-section scroll-animate">
          <div class="section-header">
            <span class="section-number">02</span>
            <h2>What We Don't Collect</h2>
          </div>
          <div class="section-content">
            <p>
              Unlike many <strong>money tracker</strong> apps, we have a strict no-collection policy.
              Here's what this <strong>budget tracker</strong> will never ask for or collect:
            </p>
            <ul class="check-list">
              <li>
                <span class="check-icon"><Icon name="check" size={16} /></span>
                <span><strong>No financial data collection</strong> — Your expenses, income, and budgets in the expense tracker stay on your device</span>
              </li>
              <li>
                <span class="check-icon"><Icon name="check" size={16} /></span>
                <span><strong>No account required</strong> — Use this money manager without email, phone, or sign-up</span>
              </li>
              <li>
                <span class="check-icon"><Icon name="check" size={16} /></span>
                <span><strong>No location tracking</strong> — We don't track where you spend or where you are</span>
              </li>
              <li>
                <span class="check-icon"><Icon name="check" size={16} /></span>
                <span><strong>No third-party analytics</strong> — No tracking of how you use the budget app</span>
              </li>
              <li>
                <span class="check-icon"><Icon name="check" size={16} /></span>
                <span><strong>No data sales</strong> — We can't sell what we don't have</span>
              </li>
            </ul>
          </div>
        </section>

        <!-- What We Do Collect -->
        <section class="privacy-section scroll-animate">
          <div class="section-header">
            <span class="section-number">03</span>
            <h2>What We Do Collect</h2>
          </div>
          <div class="section-content">
            <p>
              In the spirit of full transparency, here's the minimal data involved when using this
              <strong>spending tracker</strong>:
            </p>
            <div class="collect-grid">
              <div class="collect-card">
                <h3>Crash Reports</h3>
                <p>
                  Optional and anonymized. If enabled through Apple's settings, crash data helps us
                  improve the <strong>bill tracker</strong> stability. These contain no personal information.
                </p>
              </div>
              <div class="collect-card">
                <h3>Purchase Information</h3>
                <p>
                  Handled entirely by Apple. We receive confirmation that you subscribed to the
                  <strong>budget planner</strong>, but no payment details or Apple ID information.
                </p>
              </div>
            </div>
          </div>
        </section>

        <!-- No Bank Connections -->
        <section class="privacy-section scroll-animate">
          <div class="section-header">
            <span class="section-number">04</span>
            <h2>No Bank Connections</h2>
          </div>
          <div class="section-content">
            <p>
              MyDinero does not connect to banks, credit cards, or any financial institutions. This is
              intentional. Our <strong>expense tracker</strong> is designed for manual entry because:
            </p>
            <ul class="benefit-list">
              <li>
                <strong>Better awareness</strong> — Manually entering expenses in a <strong>money tracker</strong>
                increases financial mindfulness
              </li>
              <li>
                <strong>Zero credential risk</strong> — Your banking passwords never touch this <strong>budget app</strong>
              </li>
              <li>
                <strong>Complete privacy</strong> — No third-party aggregators can access your financial history
              </li>
            </ul>
            <p>
              When you <strong>track spending</strong> manually, you stay in complete control of your
              financial data.
            </p>
          </div>
        </section>

        <!-- Your Rights -->
        <section class="privacy-section scroll-animate">
          <div class="section-header">
            <span class="section-number">05</span>
            <h2>Your Rights & Control</h2>
          </div>
          <div class="section-content">
            <p>
              Since all your <strong>expense manager</strong> data lives on your device and in your personal
              iCloud, you have complete control:
            </p>
            <div class="rights-grid">
              <div class="right-item">
                <div class="right-icon">
                  <Icon name="export" size={20} />
                </div>
                <div>
                  <h3>Export Your Data</h3>
                  <p>Download all your <strong>budget tracker</strong> data as CSV anytime from Settings</p>
                </div>
              </div>
              <div class="right-item">
                <div class="right-icon">
                  <Icon name="categories" size={20} />
                </div>
                <div>
                  <h3>Delete Everything</h3>
                  <p>Use "Delete All Data" in Settings or simply delete the <strong>money manager</strong> app</p>
                </div>
              </div>
              <div class="right-item">
                <div class="right-icon">
                  <Icon name="cloud" size={20} />
                </div>
                <div>
                  <h3>Control Sync</h3>
                  <p>Enable or disable iCloud sync for this <strong>spending tracker</strong> anytime in iOS Settings</p>
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- Children's Privacy -->
        <section class="privacy-section scroll-animate">
          <div class="section-header">
            <span class="section-number">06</span>
            <h2>Children's Privacy</h2>
          </div>
          <div class="section-content">
            <p>
              This <strong>budget planner</strong> is not intended for users under 13 years of age. Since we
              don't collect any personal data, there is no information from children to protect. Parents
              should supervise any use of this <strong>expense tracker</strong> by minors.
            </p>
          </div>
        </section>

        <!-- Policy Updates -->
        <section class="privacy-section scroll-animate">
          <div class="section-header">
            <span class="section-number">07</span>
            <h2>Policy Updates</h2>
          </div>
          <div class="section-content">
            <p>
              We may update this privacy policy as our <strong>money tracker</strong> evolves. If we make
              significant changes, we'll notify you through the app. The current version will always be
              available on this page with the "Last updated" date at the top.
            </p>
          </div>
        </section>

        <!-- Contact -->
        <section class="privacy-section scroll-animate">
          <div class="section-header">
            <span class="section-number">08</span>
            <h2>Questions?</h2>
          </div>
          <div class="section-content">
            <p>
              If you have any questions about how this <strong>budget app</strong> handles your privacy,
              we're here to help.
            </p>
            <a href={mailtoLink} class="contact-button">
              <span>Contact Us About Privacy</span>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                <path d="M5 12h14M12 5l7 7-7 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
          </div>
        </section>
      </article>
    </div>
  </div>
</BaseLayout>

<style>
  .privacy-page {
    position: relative;
    min-height: 100vh;
    padding-top: calc(72px + var(--space-16));
    padding-bottom: var(--space-16);
  }

  /* Ambient Background */
  .page-bg {
    position: fixed;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
    z-index: -1;
  }

  .page-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.4;
    animation: float-slow 20s ease-in-out infinite;
  }

  .page-orb-1 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.08), transparent 70%);
    top: -10%;
    right: -15%;
    animation-delay: 0s;
  }

  .page-orb-2 {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.05), transparent 70%);
    bottom: 20%;
    left: -10%;
    animation-delay: -10s;
  }

  @keyframes float-slow {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(30px, -30px) scale(1.05); }
    66% { transform: translate(-20px, 20px) scale(0.95); }
  }

  .page-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse at center, black 30%, transparent 70%);
    -webkit-mask-image: radial-gradient(ellipse at center, black 30%, transparent 70%);
  }

  /* Page Header */
  .page-header {
    text-align: center;
    margin-bottom: var(--space-12);
  }

  .page-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0.03) 100%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-full);
    margin-bottom: var(--space-6);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    font-size: var(--font-size-sm);
    font-weight: 500;
    color: var(--color-white-70);
  }

  .page-title {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin-bottom: var(--space-4);
  }

  .title-highlight {
    position: relative;
    display: inline-block;
  }

  .title-highlight::after {
    content: '';
    position: absolute;
    bottom: 0.1em;
    left: 0;
    right: 0;
    height: 0.12em;
    background: linear-gradient(90deg, rgba(255, 255, 255, 0.4), rgba(255, 255, 255, 0.1));
    border-radius: var(--radius-full);
  }

  .page-updated {
    font-size: var(--font-size-sm);
    color: var(--color-white-40);
  }

  /* Privacy Content */
  .privacy-content {
    max-width: 720px;
    margin-inline: auto;
  }

  /* Hero Section */
  .privacy-hero {
    margin-bottom: var(--space-12);
  }

  .hero-card {
    position: relative;
    border-radius: var(--radius-2xl);
    overflow: hidden;
    transition: transform var(--transition-base);
  }

  .hero-card:hover {
    transform: translateY(-2px);
  }

  .card-glass {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.08) 0%,
      rgba(255, 255, 255, 0.02) 50%,
      rgba(255, 255, 255, 0.05) 100%
    );
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: var(--radius-2xl);
    box-shadow:
      0 8px 32px rgba(0, 0, 0, 0.2),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
    transition: box-shadow var(--transition-base), border-color var(--transition-base);
  }

  .hero-card:hover .card-glass {
    border-color: rgba(255, 255, 255, 0.18);
    box-shadow:
      0 12px 40px rgba(0, 0, 0, 0.25),
      inset 0 1px 0 rgba(255, 255, 255, 0.08);
  }

  .card-content {
    position: relative;
    z-index: 1;
    padding: var(--space-8);
    text-align: center;
  }

  .hero-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 64px;
    height: 64px;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.05));
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-xl);
    color: var(--color-white);
    margin-bottom: var(--space-5);
  }

  .hero-card h2 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    margin-bottom: var(--space-4);
  }

  .hero-text {
    font-size: var(--font-size-lg);
    color: var(--color-white-70);
    margin-bottom: var(--space-3);
  }

  .hero-card p {
    color: var(--color-white-60);
    line-height: 1.7;
  }

  .hero-card strong {
    color: var(--color-white-70);
  }

  /* Privacy Sections */
  .privacy-section {
    margin-bottom: var(--space-10);
    padding-bottom: var(--space-10);
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }

  .privacy-section:last-child {
    border-bottom: none;
    margin-bottom: 0;
    padding-bottom: 0;
  }

  .section-header {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    margin-bottom: var(--space-5);
  }

  .section-number {
    font-size: var(--font-size-sm);
    font-weight: 600;
    color: var(--color-white-30);
    font-family: monospace;
  }

  .section-header h2 {
    font-size: var(--font-size-xl);
    font-weight: 700;
    color: var(--color-white);
  }

  .section-content p {
    color: var(--color-white-60);
    line-height: 1.8;
    margin-bottom: var(--space-4);
  }

  .section-content p:last-child {
    margin-bottom: 0;
  }

  .section-content strong {
    color: var(--color-white-70);
    font-weight: 500;
  }

  /* Info Card */
  .info-card {
    display: flex;
    gap: var(--space-4);
    padding: var(--space-5);
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: var(--radius-xl);
    margin-top: var(--space-5);
    transition: border-color var(--transition-fast), background var(--transition-fast);
  }

  .info-card:hover {
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(255, 255, 255, 0.1);
  }

  .info-icon {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-lg);
    color: var(--color-white-60);
    transition: background var(--transition-fast), color var(--transition-fast);
  }

  .info-card:hover .info-icon {
    background: rgba(255, 255, 255, 0.08);
    color: var(--color-white-70);
  }

  .info-content h3 {
    font-size: var(--font-size-base);
    font-weight: 600;
    color: var(--color-white);
    margin-bottom: var(--space-2);
  }

  .info-content p {
    font-size: var(--font-size-sm);
    margin-bottom: 0;
  }

  /* Check List */
  .check-list {
    list-style: none;
    padding: 0;
    margin: var(--space-5) 0 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .check-list li {
    display: flex;
    gap: var(--space-3);
    padding: var(--space-4);
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-lg);
    transition: border-color var(--transition-fast), background var(--transition-fast);
  }

  .check-list li:hover {
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(255, 255, 255, 0.08);
  }

  .check-icon {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-md);
    color: var(--color-white-50);
  }

  .check-list span:last-child {
    color: var(--color-white-60);
    font-size: var(--font-size-sm);
    line-height: 1.6;
  }

  /* Collect Grid */
  .collect-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-4);
    margin-top: var(--space-5);
  }

  .collect-card {
    padding: var(--space-5);
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: var(--radius-xl);
    transition: border-color var(--transition-fast), background var(--transition-fast), transform var(--transition-fast);
  }

  .collect-card:hover {
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(255, 255, 255, 0.1);
    transform: translateY(-2px);
  }

  .collect-card h3 {
    font-size: var(--font-size-base);
    font-weight: 600;
    color: var(--color-white);
    margin-bottom: var(--space-2);
  }

  .collect-card p {
    font-size: var(--font-size-sm);
    margin-bottom: 0;
  }

  /* Benefit List */
  .benefit-list {
    list-style: none;
    padding: 0;
    margin: var(--space-4) 0;
  }

  .benefit-list li {
    position: relative;
    padding-left: var(--space-5);
    margin-bottom: var(--space-3);
    color: var(--color-white-60);
    line-height: 1.7;
  }

  .benefit-list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.6em;
    width: 6px;
    height: 6px;
    background: var(--color-white-30);
    border-radius: 50%;
  }

  .benefit-list strong {
    color: var(--color-white-70);
  }

  /* Rights Grid */
  .rights-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    margin-top: var(--space-5);
  }

  .right-item {
    display: flex;
    gap: var(--space-4);
    padding: var(--space-4);
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-lg);
    transition: border-color var(--transition-fast), background var(--transition-fast);
  }

  .right-item:hover {
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(255, 255, 255, 0.08);
  }

  .right-item:hover .right-icon {
    background: rgba(255, 255, 255, 0.08);
    color: var(--color-white-70);
  }

  .right-icon {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-md);
    color: var(--color-white-60);
    transition: background var(--transition-fast), color var(--transition-fast);
  }

  .right-item h3 {
    font-size: var(--font-size-sm);
    font-weight: 600;
    color: var(--color-white);
    margin-bottom: var(--space-1);
  }

  .right-item p {
    font-size: var(--font-size-sm);
    color: var(--color-white-50);
    margin-bottom: 0;
  }

  /* Contact Button */
  .contact-button {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    margin-top: var(--space-4);
    padding: var(--space-3) var(--space-6);
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg);
    color: var(--color-white);
    font-weight: 500;
    font-size: var(--font-size-sm);
    transition: all var(--transition-fast);
  }

  .contact-button:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.15);
  }

  .contact-button svg {
    transition: transform var(--transition-fast);
  }

  .contact-button:hover svg {
    transform: translateX(4px);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .collect-grid {
      grid-template-columns: 1fr;
    }

    .card-content {
      padding: var(--space-6);
    }

    .info-card {
      flex-direction: column;
      text-align: center;
    }

    .info-icon {
      margin-inline: auto;
    }
  }

  @media (max-width: 480px) {
    .section-header {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-2);
    }

    .check-list li {
      flex-direction: column;
      gap: var(--space-2);
    }

    .right-item {
      flex-direction: column;
      text-align: center;
    }

    .right-icon {
      margin-inline: auto;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .page-orb,
    .contact-button,
    .contact-button svg,
    .hero-card,
    .card-glass,
    .info-card,
    .info-icon,
    .check-list li,
    .collect-card,
    .right-item,
    .right-icon {
      animation: none;
      transition: none;
    }
  }
</style>
